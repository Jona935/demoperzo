<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Main bubble: mascot icon -->
    <ImageView
        android:id="@+id/ivBubble"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_claude_mascot"
        android:background="@drawable/bubble_claude"
        android:elevation="8dp"
        android:padding="4dp"
        android:contentDescription="Claude Bubble" />

    <!-- Close / dismiss button (top-right corner) -->
    <ImageView
        android:id="@+id/ivCloseBubble"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-2dp"
        android:layout_marginEnd="-2dp"
        android:src="@drawable/ic_close"
        android:background="@drawable/bg_close_btn"
        android:elevation="10dp"
        android:padding="3dp"
        android:contentDescription="Cerrar burbuja" />

</FrameLayout>
